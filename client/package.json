{
  "name": "devhitori-react-boilerplate",
  "version": "0.2.0",
  "private": true,
  "dependencies": {
    "axios": "^0.21.1",
    "faker": "^4.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router": "^5.1.0",
    "react-router-dom": "^5.1.0",
    "react-scripts": "3.0.1",
    "semantic-ui-react": "^0.88.1",
    "serve": "^11.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "prod-test": "yarn build && serve -s build -l 80",
    "prod": "yarn build && serve -s build -l 80"
  },
  "proxy": "http://localhost:9000",
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
